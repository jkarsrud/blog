{{> header}}
<div class="row">
  <div class="col-lg-8">
  {{{contents}}}
    {{#each collections.posts}}
    <article>
      <h2><a href="{{this.path}}">{{this.title}}</a></h2>
      {{{this.contents}}}
    </article>
    {{/each}}
  </div>
</div>
{{> footer}}